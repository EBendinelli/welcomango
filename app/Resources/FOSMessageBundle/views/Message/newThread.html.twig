{% extends "FOSUserBundle:Profile:show.html.twig" %}

{% block profile_content %}

    <h2>{{ participation.experience.title }} - messages</h2>

    {{ form_start(form, {'action': path('message_request', {'user_id': user.id, 'participation_id': participation.id})}) }}
        {{ form_rest(form)  }}
        <input type="submit" />
    {{ form_end(form) }}

{% endblock %}
