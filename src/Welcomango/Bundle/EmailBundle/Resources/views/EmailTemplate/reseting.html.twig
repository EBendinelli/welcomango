{% trans_default_domain 'interface' %}

{% block subject %}
    {% autoescape false %}
    {{ 'resetting.email.subject'|trans({'%username%': user.username}, 'FOSUserBundle') }}
    {% endautoescape %}
{% endblock %}

{% block body_html %}

    {% include "WelcomangoEmailBundle:EmailTemplate:template" ~ type|default('') ~ "WithParams.html.twig" with {
        'mainTitle': 'global.email'|trans,
        'coreTitle': 'email.resetting.coreTitle'|trans ,
        'SubTitle': 'email.resetting.subTitle'|trans({"%user%": user.firstname}),
        'btn': 'email.resetting.btn'|trans,
        'btn_url': confirmationUrl,
        'message' : 'email.resetting.message'|trans
    } %}


{% endblock %}
