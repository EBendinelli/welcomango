{# Complete with correct informations #}
<div class="col-sm-{{ 12/column }} m-t-20">
    {#<div class="card-city-picto" style="background-image:url({{ asset('img/welcomango-picto.png') }}">
        &#123;&#35; TODO see if we add the note in this mango. If so, add white BG to the picture &#35;&#125;
        <div class="card-picto-note">
            <h4 class="text-primary bold">{{ experience.getAverageNote }}<i class="fa fa-star fa-sm"></i></h4>
        </div>
    </div>#}
    <div id="experience_{{ experience.getId }}" class="card " >
        <a href="{{ path('front_experience_view', {'slug' : experience.slug }) }}" >
            <div class="experience-image p-b-30 p-t-50 bg-master-darkest sm-no-margin" data-pages-bg-image="{{ asset(get_banner(experience)) }}" data-bg-overlay="black" data-overlay-opacity="0.2" >
                <div class="panel-title semi-bold text-white">
                    {{ experience.getTitle }} with <span class="text-primary">{{ experience.getCreator.firstname }}</span>
                </div>
                <div class="text-center m-b-10 text-white">
                    in <span class="semi-bold">{% if experience.getCity %}{{ experience.getCity.Name }}{% endif %}</span>
                </div>
            </div>
        </a>

        <div class="panel-body p-t-0 card-content">

            <div class="m-b-10 text-center">
                {{ include("WelcomangoCoreBundle:Core:note.html.twig", { 'note': experience.getAverageNote, 'mango': true}) }}
            </div>
            <div class="dotted-line"><span></span></div>
            <div class="text-center m-t-20 m-b-20">
                <p class="fs-14">{{ experience.getDescription | truncate(200)}}</p>
            </div>
            <div class="card-price-tag bg-primary-light text-white fs-14">
                {{ experience.pricePerHour }}€/h
            </div>
        </div>
    </div>

</div>

